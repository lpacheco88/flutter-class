import 'package:flutter/material.dart';

class FavoriteScreen extends StatelessWidget {
  static const pageTitle = 'Favorites';
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('The favorites'),
    );
  }
}
